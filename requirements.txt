# ── Web Framework ──────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Autenticación JWT ───────────────────────────────────────────
python-jose[cryptography]   # crear y verificar tokens JWT
passlib[bcrypt]            # hashear contraseñas
bcrypt

# ── Base de Datos ───────────────────────────────────────────────
supabase               # cliente oficial de Supabase
asyncpg                  # driver async para PostgreSQL

# ── Validación y Settings ───────────────────────────────────────
pydantic
pydantic-settings           # leer variables de entorno (.env)
python-multipart           # para recibir formularios

# ── Reportes ────────────────────────────────────────────────────
pandas
reportlab

# ── Dev / Testing ────────────────────────────────────────────────
pytest
pytest-asyncio
httpx                     # cliente HTTP para tests
python-dotenv
